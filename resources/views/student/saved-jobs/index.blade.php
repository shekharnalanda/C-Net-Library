<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0"><meta name="robots" content="noindex,nofollow,noarchive"><title>Saved Jobs - C-Net Library</title><style>*{box-sizing:border-box}body{font-family:Inter,Arial,sans-serif;background:#f4f7fa;margin:0;color:#172033}.wrap{max-width:1050px;margin:30px auto;padding:0 18px}.top{display:flex;justify-content:space-between;align-items:center;gap:16px;margin-bottom:20px}.brand{font-size:12px;text-transform:uppercase;letter-spacing:1.3px;color:#0f766e;font-weight:800}.card{background:#fff;border:1px solid #e1e8ef;border-radius:16px;padding:20px;margin-bottom:14px;box-shadow:0 6px 22px rgba(15,23,42,.04)}.btn{display:inline-flex;align-items:center;justify-content:center;background:#102a43;color:#fff;text-decoration:none;padding:10px 14px;border-radius:10px;border:1px solid #102a43;cursor:pointer;font-weight:700}.btn.alt{background:#fff;color:#102a43;border-color:#ccd8e2}.btn.green{background:#0f766e;border-color:#0f766e}.muted{color:#718096}.actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.jobTop{display:flex;justify-content:space-between;gap:18px}.jobTop h2{margin:0 0 5px;color:#102a43}.deadline{white-space:nowrap;background:#fff7ed;color:#9a3412;border-radius:10px;padding:8px 10px;font-size:13px;font-weight:800;height:max-content}.empty{text-align:center;padding:44px 20px}.empty h2{color:#102a43}@media(max-width:700px){.top,.jobTop{align-items:flex-start;flex-direction:column}.top>div:last-child{display:flex;gap:8px;flex-wrap:wrap}.actions .btn,.actions form,.actions form button{width:100%}.actions form{flex:1 0 100%}}</style></head><body><div class="wrap"><div class="top"><div><div class="brand">Student Portal</div><h1 style="margin:2px 0">Saved Jobs</h1><div class="muted">Keep important opportunities together and apply through their official source.</div></div><div><a class="btn alt" href="{{ route('jobs.index') }}">Browse Jobs</a> <a class="btn" href="{{ route('student.dashboard') }}">Dashboard</a></div></div>@if(session('success'))<div class="card" style="border-color:#86efac;background:#f0fdf4">{{ session('success') }}</div>@endif
@forelse($jobs as $job)<div class="card"><div class="jobTop"><div><h2>{{ $job->title }}</h2><div class="muted">{{ $job->organization }} · {{ ucfirst($job->job_type) }}@if($job->location) · {{ $job->location }}@endif</div></div>@if($job->last_date)<div class="deadline">Apply by {{ $job->last_date->format('d M Y') }}</div>@endif</div>@if($job->summary)<p>{{ $job->summary }}</p>@endif<div class="actions"><a class="btn green" href="{{ route('jobs.official',$job) }}" target="_blank" rel="noopener noreferrer">Official Apply / Details ↗</a><form method="POST" action="{{ route('student.saved-jobs.destroy',$job) }}" onsubmit="return confirm('Remove this job from your saved list?')">@csrf @method('DELETE')<button class="btn alt" type="submit">Remove Saved Job</button></form></div></div>@empty<div class="card empty"><h2>No saved jobs yet</h2><p class="muted">Browse current opportunities and save the ones you want to revisit.</p><a class="btn green" href="{{ route('jobs.index') }}">Browse Career Opportunities</a></div>@endforelse<div style="margin-top:18px">{{ $jobs->links() }}</div></div></body></html>