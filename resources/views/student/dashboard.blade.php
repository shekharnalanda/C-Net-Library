<!DOCTYPE html>
<html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0"><meta name="robots" content="noindex,nofollow,noarchive"><title>My Portal - C-Net Library</title><style>
*{box-sizing:border-box}body{font-family:Inter,Arial,sans-serif;background:#f4f7fa;margin:0;color:#172033}.wrap{max-width:1180px;margin:0 auto;padding:26px 20px}.top{display:flex;justify-content:space-between;gap:18px;align-items:center;margin-bottom:22px}.brand{font-size:13px;text-transform:uppercase;letter-spacing:1.5px;color:#0f766e;font-weight:800}.actions,.quick{display:flex;gap:10px;flex-wrap:wrap}.btn{display:inline-flex;align-items:center;justify-content:center;background:#102a43;color:#fff;text-decoration:none;padding:10px 14px;border-radius:10px;border:1px solid #102a43;cursor:pointer;font-weight:700}.btn.alt{background:#fff;color:#102a43;border-color:#d5dee7}.btn.green{background:#0f766e;border-color:#0f766e}.hero{background:linear-gradient(135deg,#102a43,#164e63);color:#fff;border-radius:20px;padding:26px;margin-bottom:18px;display:flex;justify-content:space-between;gap:22px;align-items:center}.hero h1{margin:2px 0 5px;font-size:30px}.hero .muted{color:#cbd9e5}.grid{display:grid;grid-template-columns:repeat(4,1fr);gap:14px}.card{background:#fff;border:1px solid #e2e8f0;border-radius:15px;padding:19px;box-shadow:0 7px 24px rgba(15,23,42,.045);margin-bottom:18px}.metric{font-size:28px;font-weight:850;color:#102a43}.muted{color:#718096}.two{display:grid;grid-template-columns:1fr 1fr;gap:18px}.tableWrap{overflow:auto}.table{width:100%;border-collapse:collapse;min-width:500px}.table th,.table td{padding:10px;border-bottom:1px solid #edf0f4;text-align:left;font-size:14px}.table th{color:#64748b;font-size:12px;text-transform:uppercase}.status{display:inline-block;padding:4px 8px;border-radius:999px;background:#e7f7f3;color:#0f766e;font-size:12px;font-weight:800}.due{color:#b45309}.sectionTitle{margin:0 0 14px;font-size:20px}.quick{margin-bottom:18px}.quick .btn{flex:0 0 auto}@media(max-width:850px){.grid{grid-template-columns:1fr 1fr}.two{grid-template-columns:1fr}.top,.hero{align-items:flex-start;flex-direction:column}}@media(max-width:520px){.wrap{padding:18px 14px}.grid{grid-template-columns:1fr 1fr}.metric{font-size:23px}.hero h1{font-size:25px}.actions{width:100%}.actions .btn,.actions form,.actions form button{flex:1}.quick{display:grid;grid-template-columns:1fr 1fr}.quick .btn:last-child{grid-column:1/-1}}
</style></head><body><div class="wrap">
<div class="top"><div><div class="brand">C-Net Library Student Portal</div><div class="muted">{{ $student->student_code }} · {{ $student->branch?->name ?? 'C-Net Library' }}</div></div><div class="actions"><a class="btn green" href="{{ route('student.id-card') }}">Digital ID</a><form method="POST" action="{{ route('logout') }}">@csrf<button class="btn" type="submit">Logout</button></form></div></div>
<div class="hero"><div><div class="muted">Welcome back</div><h1>{{ $student->name }}</h1><div class="muted">Your membership, seat, attendance, books and study resources in one place.</div></div><div><span class="status">{{ $membership ? ucfirst($membership->status) : 'No active membership' }}</span></div></div>
<div class="quick"><a class="btn alt" href="{{ route('digital-library.index') }}">Digital Library</a><a class="btn alt" href="{{ route('student.saved-jobs.index') }}">Saved Jobs</a><a class="btn alt" href="{{ route('jobs.index') }}">Jobs & Career</a></div>
<div class="grid"><div class="card"><div class="muted">Membership valid until</div><div class="metric">{{ $membership?->expiry_date?->format('d M') ?? '—' }}</div><div class="muted">{{ $membership?->feePlan?->name ?? 'No active plan' }}</div></div><div class="card"><div class="muted">Current Seat</div><div class="metric">{{ $activeSeat?->seat?->seat_no ?? '—' }}</div><div class="muted">{{ $activeSeat?->seat?->studyHall?->name ?? 'Not allocated' }}</div></div><div class="card"><div class="muted">Fee Due</div><div class="metric {{ $due>0?'due':'' }}">₹{{ number_format($due,0) }}</div><div class="muted">Paid ₹{{ number_format($paid,0) }} / ₹{{ number_format((float)($membership?->final_fee ?? 0),0) }}</div></div><div class="card"><div class="muted">Recent Study Time</div><div class="metric">{{ number_format($studyMinutes/60,1) }}h</div><div class="muted">Last 10 attendance sessions</div></div></div>
<div class="two"><div class="card"><h2 class="sectionTitle">Membership & Seat</h2><p><strong>Plan:</strong> {{ $membership?->feePlan?->name ?? '—' }}</p><p><strong>Study slot:</strong> {{ $membership?->studySlot?->name ?? '—' }}</p><p><strong>Validity:</strong> {{ $membership?->start_date?->format('d M Y') ?? '—' }} — {{ $membership?->expiry_date?->format('d M Y') ?? '—' }}</p><p><strong>Fee:</strong> ₹{{ number_format((float)($membership?->final_fee ?? 0),2) }} · <strong>Paid:</strong> ₹{{ number_format($paid,2) }}</p><p><strong>Seat:</strong> {{ $activeSeat?->seat?->studyHall?->name ?? '—' }} / {{ $activeSeat?->seat?->seat_no ?? '—' }}</p>@if($due>0)<div class="due"><strong>₹{{ number_format($due,2) }} payment is pending.</strong> Please contact the library desk for payment/receipt assistance.</div>@endif</div>
<div class="card"><h2 class="sectionTitle">Recent Attendance</h2><div class="tableWrap"><table class="table"><thead><tr><th>Date</th><th>Check In</th><th>Check Out</th><th>Study</th></tr></thead><tbody>@forelse($student->attendances as $attendance)<tr><td>{{ $attendance->attendance_date?->format('d M Y') }}</td><td>{{ $attendance->check_in_at?->format('h:i A') ?? '—' }}</td><td>{{ $attendance->check_out_at?->format('h:i A') ?? 'Inside' }}</td><td>{{ $attendance->study_minutes }} min</td></tr>@empty<tr><td colspan="4" class="muted">No attendance recorded yet.</td></tr>@endforelse</tbody></table></div></div></div>
<div class="card"><h2 class="sectionTitle">Recent Library Activity</h2><div class="tableWrap"><table class="table"><thead><tr><th>Book</th><th>Issued</th><th>Due</th><th>Status</th></tr></thead><tbody>@forelse($student->bookIssues as $issue)<tr><td>{{ $issue->bookCopy?->book?->title ?? '—' }}</td><td>{{ $issue->issued_at?->format('d M Y') }}</td><td>{{ $issue->due_at?->format('d M Y') }}</td><td>{{ ucfirst($issue->status) }}</td></tr>@empty<tr><td colspan="4" class="muted">No recent book issues.</td></tr>@endforelse</tbody></table></div></div>
</div></body></html>